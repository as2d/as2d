{
  "name": "as2d",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "run-s asbuild:test jest watch:all",
    "watch:all": "run-p watch:as watch:test",
    "watch:test": "onchange \"build/*.wasm\" \"./src/**/*.ts\" \"./test/**/*.ts\" -- npm run jest",
    "watch:as": "onchange \"assembly/**/*.ts\" -- npm run asbuild:test",
    "jest": "jest",
    "asbuild:test": "run-p asbuild:glue asbuild:Buffer asbuild:CanvasRenderingContext2D",
    "asbuild:glue": "asc assembly/glue.ts assembly/glue.test.ts -b build/glue.test.wasm -t build/glue.test.wat --sourceMap --validate --debug --use Math=JSMath",
    "asbuild:Buffer": "asc assembly/glue.ts assembly/Buffer.test.ts -b build/Buffer.test.wasm -t build/Buffer.test.wat --sourceMap --validate --debug --use Math=JSMath",
    "asbuild:CanvasRenderingContext2D": "asc assembly/glue.ts assembly/CanvasRenderingContext2D.test.ts -b build/CanvasRenderingContext2D.test.wasm -t build/CanvasRenderingContext2D.test.wat --sourceMap --validate --debug --use Math=JSMath",
    "asbuild:optimized": "asc assembly/index.ts -b build/optimized.wasm -t build/optimized.wat --sourceMap --validate --optimize"
  },
  "author": "Joshua <tenner.joshua@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "assemblyscript": "github:assemblyscript/assemblyscript"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "@types/node": "^10.12.18",
    "cross-env": "^5.2.0",
    "jest": "^23.6.0",
    "jest-canvas-mock": "^1.1.0",
    "jest-fetch-mock": "^2.1.0",
    "npm-run-all": "^4.1.5",
    "onchange": "^5.2.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  }
}
